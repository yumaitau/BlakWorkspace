# Accessibility baseline (BW-018)

Branded overlay surfaces (portal tiles, theme, login) keep keyboard navigation, labelled controls, readable contrast, and no seizure-inducing motion. Locale stays en-AU.

Machine-readable source: `docs/brand/accessibility.yaml`. `scripts/blak_brand.a11y_failures` must return an empty list.
