# en-AU locale defaults (BW-017)

Overlay human-facing locale is **en-AU**. `scripts/blak_brand.overlay_locales` reads:

- `deploy/overlays/blak/theme-values.example.yaml`
- `deploy/overlays/blak/portal-labels.example.yaml`

Both must equal `en-AU`. Do not default the suite to en-US.
