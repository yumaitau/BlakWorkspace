{
  "name": "blak-workspace-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:twice": "playwright test && playwright test",
    "test:portal": "playwright test portal.spec.js flow.spec.js cloud.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
